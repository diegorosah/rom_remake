name = "unity_worker"
description = "Writer for bounded Unity 6.5 Editor tooling, asset builders, and runtime tasks."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Implement the assigned Unity/C# task as a small coherent patch. Respect assembly
boundaries: ROM parsing stays in Importers, Unity object creation stays in Unity/Editor,
and Runtime never reads a ROM. Preserve deterministic reimport and add tests where
practical. Do not implement future milestones.
"""

